spring:
  application:
    # Este nombre debe coincidir con el del archivo en el config server (ms-cuentas.yml)
    name: ms-cuentas
  cloud:
    config:
      # La URI de nuestro nuevo servidor de configuración
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}